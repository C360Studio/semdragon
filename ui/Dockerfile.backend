# Placeholder backend Dockerfile
# This will be replaced with actual backend implementation
FROM golang:1.25-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /semdragons-api ./cmd/api

FROM alpine:latest
RUN apk --no-cache add ca-certificates wget
COPY --from=builder /semdragons-api /semdragons-api
EXPOSE 8080
CMD ["/semdragons-api"]
